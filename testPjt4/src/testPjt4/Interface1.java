package testPjt4;

public interface Interface1 {

	public void funA(); //이처럼 인터페이스는 어떠한 메서드정의 없이 선언만 해준다
	// 그래서 이것을 인터페이스는 작업명세서다 라고 이야기 하는것 
	// 이렇게 선언되어있는 메서드는 정의부가 없기 때문에 
	// 얘를 호출해서 쓰는 그 자체는 의미가 없는것
	// 이를 구현하는 것은 InterfaceA를 구현해내는 클래스에서 정의를 새롭게해서 구현

	
}
